{
  "name": "nishaaj-islam-portfolio-site",
  "version": "2.0.0",
  "description": "Static portfolio site (build from site.config.js via build.js)",
  "private": true,
  "scripts": {
    "build": "node build.js"
  }
}
